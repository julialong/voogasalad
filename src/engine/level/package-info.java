/**
 * Contains the Level interface and BasicLevel class that implements it. The level is essentially a 
 * container for all GameEntities and all of those entities can be updated (as in move 
 * autonomously) by calling BasicLevel.update().
 */
package engine.level;