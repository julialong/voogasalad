/**
 * Contains the Controls class that allows the mapping of KeyCodes to actions.
 * Also contains Action abstract class and a number of subclasses that extend it. 
 * These actions define player movements or actions that are mapped to KeyCodes.
 */
package engine.controls;