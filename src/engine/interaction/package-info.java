/**
 * Contains the Interaction interface that is implemented by many Interaction type classes
 * that define how objects interact. Each interactions takes in a source and target GameEntity
 * where the target is affected by the source in some way (such as a target player not falling 
 * through a source block).
 */
package engine.interaction;